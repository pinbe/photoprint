name: Build and Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          repository: pinbe/Plinn.git
          ref: v0.9.0
          path: Plinn

      - uses: actions/checkout@v7
        with:
          repository: pinbe/Portfolio.git
          ref: cbb
          path: Portfolio

      - uses: actions/checkout@v7
        with:
          repository: pinbe/photoprint.git
          ref: cbb
          path: photoprint

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 25.8.1
      
      - name: Portfolio Build
        working-directory: ./Portfolio/Products/Portfolio/ts
        run: |
          npm ci
          npx webpack --config webpack.config.js

      - name: photoprint Build
        working-directory: ./photoprint/Products/photoprint/ts
        run: |
          npm ci
          npx webpack --config webpack.config.js

      - name: Cleaning up and creating tarball
        run: |
          rm -rf photoprint/Products/photoprint/ts/node_modules
          rm -rf photoprint/.git*
          tar -cJf build.tar.xz photoprint/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: build.tar.xz
